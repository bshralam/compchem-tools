feat: implement Tanimoto similarity ranking

fix: handle empty SMILES strings in ligand parser

docs: update README with installation instructions

style: reformat scripts with black

refactor: separate descriptor generation into its own module

test: add unit test for fingerprint similarity

chore: add .gitignore and requirements.txt

